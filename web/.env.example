# Origin of the UroApp backend. Leave unset for local dev — Vite proxies /api
# to localhost:3002 (see vite.config.js). Required for the deployed static site,
# which has no proxy and would otherwise answer /api requests with HTML.
VITE_API_BASE=https://uroapp-backend.onrender.com
